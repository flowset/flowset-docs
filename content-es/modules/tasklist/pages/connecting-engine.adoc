= Conexión al motor BPM

*Flowset Tasklist* utiliza la API REST de los motores BPM para trabajar con procesos y tareas de usuario.
La aplicación no requiere su propio backend y se conecta directamente al motor BPM existente a través de la configuración del entorno.

== Configuración de la conexión

Para establecer una conexión con el motor, debes especificar las variables de entorno.
Se pueden definir en el archivo `.env`, `.env.local`, o mediante parámetros de ejecución del contenedor Docker.

[cols="2,3,3", options="header"]
|===
| Variable
| Descripción
| Valor de ejemplo

| `VITE_BPM_ENGINE_API_URL`
| URL completa de la API REST del motor BPM
| `http://localhost:8080/engine-rest`

| `VITE_BPM_ENGINE_TYPE`
| Tipo de motor conectado
Valores admitidos: `CAMUNDA_7`, `OPERATON`
| `OPERATON`
|===

[NOTE]
====
Para el desarrollo local, se recomienda crear un archivo `.env.local` en la raíz del proyecto y copiar en él el contenido de `env.example`.
Esto facilita la gestión de las variables de entorno sin modificar el código fuente.
====

== Configuración de usuarios

Flowset Tasklist utiliza *autenticación básica*, por lo que los usuarios y sus contraseñas se configuran en el motor BPM.
La aplicación no gestiona usuarios por sí misma.

=== Ejemplo de configuración de usuarios en Camunda 7

. Abre *Camunda WebApps* en el navegador
. Inicia sesión con un usuario administrador
. Ve a *Admin → Users*
. Haz clic en *Create New User* y completa el formulario
. Después de crear el usuario, añádelo a un grupo con los siguientes permisos:
* User
* Process Definition
* Process Instance
* Task
* Historic Task Instance

[TIP]
====
Si usas *Operaton*, el procedimiento es similar.
Lo importante es asegurarte de que el usuario tenga permisos para leer y ejecutar procesos y tareas.
====

== Verificación de la conexión

Después de configurar y ejecutar la aplicación, ve a `http://localhost:3000`
e inicia sesión utilizando las credenciales del motor BPM.
Si la conexión está configurada correctamente, aparecerá una lista de tareas activas.